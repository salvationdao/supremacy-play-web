import { Typography } from "@mui/material"

export const BattleReplayDetails = ({ battleNumber, arenaGID }: { battleNumber: string, arenaGID: string }) => {
    return <Typography>TODO: get video for replay ID {`<${battleNumber}>`}</Typography>
}
