import { Typography } from "@mui/material"

export const BattleReplayDetails = ({ replayID }: { replayID: string }) => {
    return <Typography>TODO: get video for replay ID {`<${replayID}>`}</Typography>
}
