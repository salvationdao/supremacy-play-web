import { Fade, Stack, Typography } from "@mui/material"

interface test {
    first: string
    second: string
}

export const Socials = () => {
    return (
        <Fade in>
            <Stack>
                <Typography>Socials</Typography>
            </Stack>
        </Fade>
    )
}
